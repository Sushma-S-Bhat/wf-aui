import * as React from "react";
import * as Types from "./types";

declare function EventQa(props: {
  as?: React.ElementType;
  textAnswer?: React.ReactNode;
  textQuestion?: React.ReactNode;
}): React.JSX.Element;
